#include <bits/stdc++.h>
using namespace std;

int main(){
    int W,H,x,y;
    cin >> W >> H >> x >> y;
    int flag = 0;
    double area = 0;
    if(x*H==y*W){
        flag = 1;
    } else if(x*H>y*W){
        area = 
    }
    
    return 0;

}